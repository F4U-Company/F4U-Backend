# ============================================
# ⚠️ IMPORTANTE: 
# 1. Copiar este archivo como .env
# 2. Completar con tus valores reales
# 3. NUNCA subir .env a Git
# ============================================

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Reemplaza con los valores de tu Azure SQL Server
DB_URL=jdbc:sqlserver://tu-servidor.database.windows.net:1433;database=tu-base-datos;encrypt=true;trustServerCertificate=false;loginTimeout=30;
DB_USERNAME=tu-usuario-admin
DB_PASSWORD=tu-password-segura

# Database Pool (opcional)
DB_POOL_SIZE=10
DB_MIN_IDLE=2
DB_CONNECTION_TIMEOUT=20000

# ============================================
# JPA CONFIGURATION (opcional)
# ============================================
JPA_DDL_AUTO=update
JPA_SHOW_SQL=false

# ============================================
# SERVER CONFIGURATION (opcional)
# ============================================
SERVER_PORT=8080

# ============================================
# AZURE AD / MICROSOFT ENTRA ID
# ============================================
# Obtén estos valores de Azure Portal → Azure AD → App registrations → Tu aplicación
AZURE_TENANT_ID=tu-tenant-id-aqui
AZURE_CLIENT_ID=tu-client-id-aqui
AZURE_CLIENT_SECRET=tu-client-secret-aqui

# Azure Issuer URI (reemplaza {TENANT_ID} con tu Tenant ID)
AZURE_ISSUER_URI=https://login.microsoftonline.com/{TENANT_ID}/v2.0

# Azure Audience (reemplaza {CLIENT_ID} con tu Client ID)
AZURE_AUDIENCE=api://{CLIENT_ID}

# ============================================
# CORS CONFIGURATION
# ============================================
# En desarrollo: localhost
# En producción: tu dominio de Azure Static Web App
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174,https://tu-app.azurestaticapps.net
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=*

# ============================================
# EMAIL CONFIGURATION
# ============================================
# Para Gmail, usa estos valores:
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=tu-correo@gmail.com
# MAIL_PASSWORD=tu-contraseña-de-aplicacion (no tu contraseña normal)
#
# Para Outlook/Hotmail, usa estos valores:
# MAIL_HOST=smtp-mail.outlook.com
# MAIL_PORT=587
# MAIL_USERNAME=tu-correo@outlook.com
# MAIL_PASSWORD=tu-contraseña
#
# Para otros proveedores, consulta su documentación SMTP
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=tu-correo@gmail.com
MAIL_PASSWORD=tu-contraseña-de-aplicacion
